<?xml version="1.0" encoding="utf-8"?>
<!-- layout for each item in recyclerView's List for all
fragments from drawerLayout except InstalledAppsFragment -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="83dp"
    android:orientation="horizontal"
    android:padding="3dp"
    android:weightSum="1">

    <TextView
        android:id="@+id/text_recycler_item_description"
        android:fontFamily="serif-monospace"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        style="@style/MyRecyclerItemsTextviews"/>

    <TextView
        android:id="@+id/text_recycler_item_solution"
        android:fontFamily="cursive"
        android:textColor="@color/colorPrimary"
        android:textStyle="bold|italic"
        style="@style/MyRecyclerItemsTextviews"/>

</LinearLayout>